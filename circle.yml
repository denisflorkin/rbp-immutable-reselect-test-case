#machine:
#  pre:
#  override:
#  post:

dependencies:
  pre:
    - npm --version
    - npm install -g npm
    - npm --version
    - node --version
    - node install -g n
    - n latest
    - node --version
  override:
    - npm install

database:
  override:
    - echo ''
    
test:
  override:
    - npm run test
  post:
    - npm run build
