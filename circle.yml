#machine:
#  pre:
#  override:
#  post:

dependencies:
  pre:
    - npm --version
    - npm install -g npm
    - npm --version
    - node --version
#    - npm install -g n
#    - sudo n latest
#    - node --version
  override:
    - npm install

    
test:
  override:
    - npm run test && build 
#  post: # using `post:` it seems we can run the build even if the test fails, this might sometimes be insightfull...
#    - npm run build
