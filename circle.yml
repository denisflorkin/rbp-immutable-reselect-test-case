#machine:
#  pre:
#  override:
#  post:

dependencies:
  pre:
    - npm --version
    - npm install -g npm
    - npm --version
    - node --version
    - npm install -g n
    - sudo n latest
    - node --version
  override:
    - npm install

database:
  override:
    - echo ''
    
test:
  override:
    - npm run test
  post:
    - npm run build
